package com.qweex.smashuphelper;

/*
    - list factions by expansion; each faction has a checkmark
    - each expansion has a checkmark; disable (NOT uncheck) its expansions

  - Settings
    - Taboos? (e.g. no zombots)
      - "randomly" or "ever"
 */

import android.os.Bundle;
import android.support.v7.app.AppCompatActivity;

public class Factions extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
    }


}
